"use client";
/* eslint-disable react-hooks/exhaustive-deps */

import { RefreshCw, UserCog } from "lucide-react";
import { useEffect, useState } from "react";

import { AdminGuard } from "@/components/admin/AdminGuard";
import { PermissionGate } from "@/components/admin/PermissionGate";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { useAuth } from "@/hooks/use-auth";
import type { UserProfile } from "@/types/auth";

export default function UsersPage() { const { user } = useAuth(); const [users, setUsers] = useState<UserProfile[]>([]); const [loading, setLoading] = useState(true); const [error, setError] = useState(""); async function load() { if (!user) return; setLoading(true); try { const response = await fetch("/api/admin/users", { headers: { Authorization: `Bearer ${await user.getIdToken()}` } }); const body = await response.json().catch(() => ({})) as { users?: UserProfile[]; error?: string }; if (!response.ok) throw new Error(body.error ?? "No se pudieron cargar los usuarios."); setUsers(body.users ?? []); } catch (loadError) { setError(loadError instanceof Error ? loadError.message : "No se pudieron cargar los usuarios."); } finally { setLoading(false); } } useEffect(() => { void load(); }, [user]); async function toggleActive(account: UserProfile) { if (!user || !window.confirm(`¿${account.active ? "Desactivar" : "Activar"} a ${account.displayName ?? account.email}?`)) return; const response = await fetch(`/api/admin/users/${encodeURIComponent(account.uid)}`, { method: "PATCH", headers: { "content-type": "application/json", Authorization: `Bearer ${await user.getIdToken()}` }, body: JSON.stringify({ active: !account.active }) }); const body = await response.json().catch(() => ({})) as { user?: UserProfile; error?: string }; if (!response.ok || !body.user) { setError(body.error ?? "No se pudo actualizar el usuario."); return; } setUsers((items) => items.map((item) => item.uid === account.uid ? body.user as UserProfile : item)); } return <AdminGuard permission="usuarios.read"><div className="space-y-6"><div className="flex items-end justify-between"><div><p className="text-xs font-bold uppercase tracking-[0.2em] text-cyan-700">Acceso</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Usuarios</h1></div><Button variant="outline" onClick={() => void load()} disabled={loading}><RefreshCw className={loading ? "animate-spin" : ""} /> Actualizar</Button></div>{error && <p role="alert" className="rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-800">{error}</p>}{loading ? <div className="h-40 animate-pulse rounded-xl bg-white" /> : <div className="grid gap-3">{users.map((account) => <Card key={account.uid} className="border-slate-200 bg-white shadow-sm"><CardContent className="flex flex-col gap-4 p-5 sm:flex-row sm:items-center sm:justify-between"><div className="flex items-center gap-3"><span className="rounded-full bg-slate-100 p-3 text-slate-700"><UserCog className="h-5 w-5" /></span><div><p className="font-semibold text-slate-900">{account.displayName ?? "Sin nombre"}</p><p className="text-sm text-slate-500">{account.email} · {account.accountType}</p></div></div><div className="flex items-center gap-3"><Badge variant={account.active ? "default" : "secondary"}>{account.active ? "Activo" : "Inactivo"}</Badge><PermissionGate permission="usuarios.manage"><Button variant="outline" size="sm" onClick={() => void toggleActive(account)}>{account.active ? "Desactivar" : "Activar"}</Button></PermissionGate></div></CardContent></Card>)}</div>}</div></AdminGuard>; }
